<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kienenwayrynen.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/small_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.Login"
            android:label="@string/login_button_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.Registration"
            android:label="@string/register_button_label">

        </activity>
        <activity
            android:name=".activities.DisabilityProfile"
            android:label="@string/disability_profile" />
        <activity
            android:name=".activities.RouteFinder"
            android:label="@string/route_finder" />
    </application>

</manifest>